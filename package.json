{
  "name": "backend",
  "version": "0.0.1",
  "description": "\"\"",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "\"echo \\ \"Error: no test specifified\\\" && exit 1\"",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf build && cd ../rendering-collections && npm run build --prod && cp -r build ../notesBackEnd"
  },
  "author": "Homer Bacanto",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.12.3"
  },
  "devDependencies": {
    "cors": "^2.8.5",
    "nodemon": "^2.0.7"
  }
}
